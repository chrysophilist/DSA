//ABSTRACT DATATYPE(ADT): abstract means HIDING internal details
        //1. Representation of data:
                    //for reprenting the data, we need:
                            //1. space for storing elements. Ex: using Array, linked list
                            //2. Capacity 
                            //3. size
        //2. Operation on data:
                    //add(x)/append(x): adding an element to the end of the list
                    //add(index,x)/insert(index,x): adding element at a given index
                    //remove(index): removing an element from the given index  
                    //set(index,x)/replace(index,x): replacing an element to the given index
                    //get(index): knowing an element at the given index
                    //search(key)/contains(key): searching an element to find its index if it is present in the list
                    //sort(): sorting the list



